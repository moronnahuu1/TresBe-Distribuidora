export class Options{
    id: string;
    name: string;
    productID: string;

    constructor(id: string, name: string, productID: string){
        this.id = id;
        this.name = name;
        this.productID = productID;
    }
}